{% extends 'CaiWebBundle::base.html.twig' %}

{% block body %}
    <h1>Editar Documento</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('documento_index') }}">Volver</a>
        </li>
    </ul>
{% endblock %}
